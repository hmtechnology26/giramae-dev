
import React from 'react';

// Este componente não é mais necessário pois usamos toast direto no hook
// Mantido para compatibilidade
export const NotificationToast: React.FC = () => {
  return null;
};
